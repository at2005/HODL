
name: HODL-build

on: [push]

jobs:
   build-and-test:
      runs-on: ubuntu-latest
      steps:
         - uses: actions/checkout@v3
         - run: mkdir bin
         - run: make
         - run: bin/hodl --target qasm -o out.qasm data/grover.hodl


